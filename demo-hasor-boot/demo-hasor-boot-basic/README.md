# Demo Hasor Boot Basic

Build the executable archive:

```bash
../../gradlew :demo-hasor-boot-basic:bootJar
```

Run it:

```bash
java -jar build/libs/demo-hasor-boot-basic-5.0.1-SNAPSHOT-boot.jar demo
```
