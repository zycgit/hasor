plugins {
    id 'java-library'
    id 'maven-publish'
}

ext.pomName = 'Hasor-Boot-Loader'
description = 'Hasor Boot executable archive loader.'

// Bootstrap must remain runnable before nested application dependencies are available.
dependencies {
    compileOnly "net.hasor:cobble-all:${property('lib.cobble')}"
}